name: CI Smoke Test
on:
  push:
    branches:
      - ci/add-pest-workflow
      - main

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print hello
        run: |
          echo "CI smoke: repo actions are working"
